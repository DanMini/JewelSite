@model IEnumerable<BraceletCategory>

@{
    ViewBag.Title = "Админка — Категория браслетов";
}

@await Component.InvokeAsync("AdminPanel")

<div class="adminbutton">
    <a asp-controller="Panel" asp-action="EditBraceletCategory">Создать категорию</a>
</div>

<div class="row text-center header animate-in" data-anim-type="fade-in-up">
    <h3 style="margin-top: 20px">Браслеты</h3>
</div>

<div style="overflow-x:auto;">
    <table>
        @foreach (var category in Model)
        {
            <tr>
                <th>@category.Name</th>
                <th><a asp-controller="Panel" asp-action="EditBraceletCategory" asp-route-id="@category.Id">Изменить</a></th>
                <th><a asp-controller="Panel" asp-action="RemoveBraceletCategory" asp-route-id="@category.Id">Удалить</a></th>
            </tr>
        }
    </table>
</div>